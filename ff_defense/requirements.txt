requests>=2.31
beautifulsoup4>=4.12
# fetch_schedule.py / fetch_nflverse_epa.py / fetch_def_rating.py read
# nflverse's gzipped release CSVs -- pandas is a hard runtime dependency,
# not a dev convenience. It was missing here while those columns still came
# from FTN and Subvertadown, so CI would have failed on the first run.
pandas>=2.0
google-auth>=2.28
google-api-python-client>=2.120
